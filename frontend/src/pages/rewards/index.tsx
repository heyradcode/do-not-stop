import React from 'react';
import Rewards from '@components/rewards';

/** Top-level `/rewards` page — season entitlements and the claim (§I). */
const RewardsPage: React.FC = () => <Rewards />;

export default RewardsPage;
