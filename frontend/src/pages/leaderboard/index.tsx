import React from 'react';
import Leaderboard from '@components/leaderboard';

/** Top-level `/leaderboard` page — pet and player rankings (read-only). */
const LeaderboardPage: React.FC = () => <Leaderboard />;

export default LeaderboardPage;
