import React from 'react';
import Inventory from '@components/inventory';

/** Top-level `/inventory` page — the player's items, and anything waiting to be claimed. */
const InventoryPage: React.FC = () => <Inventory />;

export default InventoryPage;
