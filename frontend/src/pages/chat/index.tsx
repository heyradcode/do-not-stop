import React from 'react';
import Chat from '@components/chat';

/** Top-level `/messages` page — private threads with married-pet counterparts. */
const ChatPage: React.FC = () => <Chat />;

export default ChatPage;
