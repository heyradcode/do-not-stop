{
  "name": "cryptopets-subgraph-solana",
  "private": true,
  "license": "MIT",
  "scripts": {
    "configure": "node scripts/prepare-subgraph.mjs",
    "build": "pnpm configure && graph build",
    "deploy": "graph deploy cryptopets-solana"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.98.1"
  }
}