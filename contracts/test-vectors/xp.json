{
  "description": "XP formula + same-opponent decay golden vectors (plan §3.4, §7). calcXpCases mirror GameLogic._calcXp / settle_battle::calc_xp: xp = floor(baseXp * clamp(100 + 10*(oppLevel - myLevel), 0, 200) / 100). decaySequences mirror PetCore.recordBattleOpponent / PetAccount::record_battle_opponent applied to a fresh pet (lastOpponentId = 0, streak = 0); expectedXp applies `calcXp(baseXp, 10, 10) >> decayShift` (myLevel == oppLevel == 10, so the base multiplier is always 100).",
  "calcXpCases": [
    { "name": "delta-zero",                 "baseXp": 100, "myLevel": 10, "oppLevel": 10, "expectedXp": 100 },
    { "name": "delta-plus10-cap-boundary",  "baseXp": 100, "myLevel": 10, "oppLevel": 20, "expectedXp": 200 },
    { "name": "delta-plus11-clamped",       "baseXp": 100, "myLevel": 10, "oppLevel": 21, "expectedXp": 200 },
    { "name": "delta-minus10-zero-boundary","baseXp": 100, "myLevel": 20, "oppLevel": 10, "expectedXp": 0 },
    { "name": "delta-minus11-clamped",      "baseXp": 100, "myLevel": 21, "oppLevel": 10, "expectedXp": 0 },
    { "name": "delta-minus5-loser-xp",      "baseXp": 25,  "myLevel": 15, "oppLevel": 10, "expectedXp": 12 },
    { "name": "delta-plus5-loser-xp",       "baseXp": 25,  "myLevel": 10, "oppLevel": 15, "expectedXp": 37 },
    { "name": "delta-plus15-clamped-loser-xp", "baseXp": 25, "myLevel": 5, "oppLevel": 20, "expectedXp": 50 }
  ],
  "decaySequences": [
    {
      "name": "repeat-then-switch-then-repeat",
      "opponentIds": [5, 5, 5, 7, 5],
      "expectedDecayShifts": [0, 1, 2, 0, 0],
      "baseXp": 100,
      "expectedXp": [100, 50, 25, 100, 100]
    },
    {
      "name": "switch-twice-then-repeat",
      "opponentIds": [3, 4, 3, 3],
      "expectedDecayShifts": [0, 0, 0, 1],
      "baseXp": 100,
      "expectedXp": [100, 100, 100, 50]
    }
  ]
}
