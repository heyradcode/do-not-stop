module.exports = {
  root: true,
  extends: '@react-native',
  // Generated by `jest --coverage`. Already gitignored, but eslint has no view of
  // that and its bundled report scripts trip the eslint-comments rules, so a
  // coverage run would otherwise turn a clean lint into four warnings.
  ignorePatterns: ['coverage/'],
};
